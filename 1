Here's the JavaScript function to find the maximum number in an array:

```javascript
function findMax(numbers) {
  let max = numbers[0]; // start with the first number in the array as the max
  for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > max) {
      max = numbers[i]; // if the current number is greater than max, update max
    }
  }
  return max;
}
```
