```javascript
function calculateArea(length, width) {
  let area = length * width;
  return area;
}
```

eg : This function can be used to calculate the area of a rectangle with any length and width, like this:

```javascript
let length = 7;
let width = 6;
let area = calculateArea(length, width);
console.log(area); // output: 42
``` 

Alternatively, the function can also be called directly with the values of length and width, like this:

```javascript
let area = calculateArea(7, 6);
console.log(area); // output: 42
``` 
